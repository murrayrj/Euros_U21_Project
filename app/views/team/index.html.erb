<h1>Teams</h1>
<ul>
  <% @teams.each do |team| %>
    <li class="team-li"><%= image_tag 'http://img.uefa.com/imgml/flags/70x70/' + team.code + '.png' %>  <%= link_to(team.name, team_path(team.id), class: 'team-link')%></li>
  <% end %>
</ul>